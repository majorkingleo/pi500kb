#!/usr/bin/env bash
#
# install-service.sh - Install pi400kb binary, hook script, and systemd service
#
# AI generated by GitHub Copilot v1.0
# End of AI generated code

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BINARY="${SCRIPT_DIR}/build/pi400kb"
HOOK_SRC="${SCRIPT_DIR}/hook.sh"
SERVICE_SRC="${SCRIPT_DIR}/pi400kb.service"

BIN_DST="/usr/local/bin/pi400kb"
HOOK_DIR="/usr/share/pi400kb"
HOOK_DST="${HOOK_DIR}/hook.sh"
SERVICE_DST="/etc/systemd/system/pi400kb.service"

# Check prerequisites
if [ ! -f "${BINARY}" ]; then
    echo "Error: Binary not found at ${BINARY}"
    echo "Run ./make.sh first to build the project."
    exit 1
fi

if [ ! -f "${HOOK_SRC}" ]; then
    echo "Error: hook.sh not found at ${HOOK_SRC}"
    exit 1
fi

# --- Install binary ---
echo "Installing binary to ${BIN_DST}..."
install -Dm755 "${BINARY}" "${BIN_DST}"

# --- Install hook script ---
echo "Installing hook script to ${HOOK_DST}..."
install -Dm755 "${HOOK_SRC}" "${HOOK_DST}"

# --- Generate systemd service file ---
echo "Installing systemd service to ${SERVICE_DST}..."
cat > "${SERVICE_DST}" <<- SERVICE
[Unit]
Description=pi400kb USB OTG Keyboard & Mouse forwarding
Documentation=https://github.com/martins/pi400kb
After=multi-user.target

[Service]
ExecStart=${BIN_DST} --hook-path ${HOOK_DST}
User=root
Group=root
Type=simple
Restart=on-failure

[Install]
WantedBy=multi-user.target
SERVICE

# --- Reload systemd and enable service ---
echo "Reloading systemd daemon..."
systemctl daemon-reload

echo "Enabling pi400kb service (starts on boot)..."
systemctl enable pi400kb

echo "Starting pi400kb service..."
systemctl start pi400kb

echo ""
echo "Installation complete!"
echo "  Binary:      ${BIN_DST}"
echo "  Hook script: ${HOOK_DST}"
echo "  Service:     ${SERVICE_DST}"
echo ""
echo "Status:"
systemctl status pi400kb --no-pager 2>&1 || true
